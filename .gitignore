# Entornos virtuales
.venv/
venv/

# Python
__pycache__/
*.pyc

# Bases de datos locales (NUNCA subir datos reales)
*.db
*.db.*.bak
*.bak

# Entorno y logs
.env
*.log

# Config local del listener (opt-in de proyectos reales; no versionar)
listener/config.json

# Config local del observer (opt-in de proyectos reales; no versionar)
observer/config.json

# Generado por setup.ps1: bloque mcpServers con rutas absolutas locales
mcp-servers.generated.json

# Seed real de proyectos (copia de la plantilla; puede tener rutas/nombres reales)
seed-projects.json
