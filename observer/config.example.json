{
  "_comment": "Copia este archivo a config.json (gitignored). 'projects' VACIO = captura observaciones de TODOS los proyectos registrados en el hub; con nombres, solo esos. La captura es local (hub.db), deterministica y sin costo de modelo.",
  "projects": [],
  "max_files": 40,
  "first_prompt_max_chars": 400,
  "retention_days": 90,
  "inject_context": true,
  "inject_max_chars": 1200,
  "inject_observations": 3
}
